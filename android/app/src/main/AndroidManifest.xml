<?xml version="1.0" encoding="utf-8"?>
<!-- release/主 manifest：明文 HTTP 一律禁止（公网必须 HTTPS，见 docs/deploy/HTTPS.md）。
     局域网调试明文由 app/src/debug/AndroidManifest.xml 单独放开，仅限 debug 包。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CNStockBot"
        android:usesCleartextTraffic="false">

        <!-- 启动 Activity：有已保存的服务器地址时直接跳 MainActivity，否则展示配置页 -->
        <activity
            android:name=".ServerConfigActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>

</manifest>
