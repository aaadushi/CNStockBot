# get_stock_financials

查询个股财务报表摘要（按报告期的营收、净利润、总资产等关键指标），供 LLM 解读基本面。

## 何时触发
- "茅台最近业绩怎么样" / "帮我看看 300750 的财报" / "这家公司赚钱吗"

## 数据来源
走 Python 数据微服务（AKShare 的 `stock_financial_abstract`，新浪财经财务摘要），
需先启动 `data-service/`。默认返回最近 4 个报告期，数值为带"元"单位的原始字符串
（未做数值清洗），LLM 直接阅读即可；未启动时技能会返回明确的错误提示。
